# 2026-04-04: Feature 127 Enhanced Charts & Visualizations — Slices 1 & 2

**Scribe:** Scribe  
**Requested by:** Fortinbra  
**Branch:** squad  
**Session:** Feature 127, Slices 1 & 2

---

## Summary

Feature 127 (Enhanced Charts & Visualizations) foundational infrastructure completed across two slices. Four agent turns delivered clean, tested, zero-warning code. Total Client test count advanced from 2,698 → 2,729 (+31 new tests).

---

## Agents & Work Done

### Alfred — Slice 2: ChartDataService Models + Interface

Created 7 model types and the `IChartDataService` interface under `BudgetExperiment.Client`:

- **Records:** `HeatmapDataPoint`, `WaterfallSegment`, `CandlestickDataPoint`, `BoxPlotSummary`, `DailyBalanceDto`
- **Enums:** `HeatmapGrouping`, `CandlestickInterval`
- **Interface:** `IChartDataService` — 4 methods (`BuildSpendingHeatmap`, `BuildBudgetWaterfall`, `BuildBalanceCandlesticks`, `BuildCategoryDistributions`)
- **Namespace:** `BudgetExperiment.Client.Components.Charts.Models`

Key design choice: `DailyBalanceDto` created as a client-local flat record (`DateOnly Date, decimal Balance`) rather than exposing `DailyBalanceSummaryDto` from Contracts, which carries multiple MoneyDto balance fields unsuited to direct chart calculation.

---

### Barbara — Slice 2: 20 RED Tests for ChartDataService

Wrote comprehensive RED-phase test suite (`ChartDataServiceTests.cs`) covering all four service methods:

- **BuildSpendingHeatmap** (4 tests): 7-row invariant, absolute amounts, same-day aggregation, multi-week separation
- **BuildBudgetWaterfall** (5 tests): spending sort order, cumulative running totals, net segment placement, negative net, MoneyDto unwrapping
- **BuildBalanceCandlesticks** (3 tests): Open/Close chronology, `IsBullish` derivation, multi-month ordering
- **BuildCategoryDistributions** (8 tests): Tukey's hinges, IQR outlier detection, `monthsBack` relative reference, per-category grouping

---

### Lucius — Slice 2: ChartDataService Implementation (GREEN)

Implemented `ChartDataService` passing all 20 RED tests. Registered in DI. Fixed 2 StyleCop violations (SA1512, SA1515) in test file.

Notable algorithms:
- `DayOfWeek.Sunday` remapped to heatmap row 6 (Mon=0…Sun=6)
- WeekIndex as 0-based offset from earliest transaction's Monday
- Waterfall sorted ascending by `Amount.Amount` (all-negative = descending by absolute value)
- Tukey's hinges with shared `ComputeMedian` helper
- `monthsBack` reference = `list.Max(t => t.Date)` — deterministic in tests

**Result:** 20/20 tests GREEN. Full suite: 2718 passed.

---

### Alfred — Slice 1: Blazor-ApexCharts + ChartThemeService + ChartColorProvider

Added `Blazor-ApexCharts` v6.1.0 to the Client project and created the theming infrastructure for Tier 3 charts (Treemap, Radar):

- **`ChartThemeService`** — maps BudgetExperiment theme names to ApexCharts `Mode` + palette. Dark themes: `dark` and `vscode-dark`. `system` defaults to light (cannot resolve browser OS theme from C# without async JS interop).
- **`ChartColorProvider`** — provides palette arrays and per-category colour assignment (hash-based for unknown categories).
- **6 unit tests** for `ChartThemeService`, **5 unit tests** for `ChartColorProvider` — all passed.
- `AddApexCharts()` registered in `Program.cs`. No manual `<script>` tag required (v6.x ES module lazy loading).

Bundle impact: ~80 KB gzipped (Tier 3 only). Tier 1 & 2 charts remain zero-JS-dependency.

**Result:** 11 new tests GREEN. Full suite: 2729 passed.

---

## Test Count Progression

| Checkpoint | Client Tests |
|------------|-------------|
| Session start | 2,698 |
| After Lucius (Slice 2 GREEN) | 2,718 |
| After Alfred (Slice 1 + ChartThemeService) | 2,729 |
| Session end | **2,729** |

---

## Architectural Outcomes

- `IChartDataService` established as clean separation between chart components and data computation
- `ChartThemeService` + `ChartColorProvider` enable consistent theming across SVG (Tier 1/2) and ApexCharts (Tier 3)
- CSS custom property values hardcoded in C# (cannot read CSS vars from .NET) — must track design token changes
- Hybrid strategy validated: Tier 1/2 self-implement (zero JS), Tier 3 via library (~80 KB gzip)

---

## Next Steps (Slice 3+)

- **Slice 3:** Heatmap + Scatter (Tier 1, self-implement SVG)
- **Slice 4:** Stacked Area + Radial Bar + Candlestick (Tier 1, self-implement SVG)
- **Slice 5:** Waterfall + Box Plot (Tier 2, extra testing discipline)
- **Slice 6:** Treemap + Radar (Tier 3, ApexCharts components — infrastructure now in place)
