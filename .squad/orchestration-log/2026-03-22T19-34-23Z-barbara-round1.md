# Orchestration: Barbara (Round 1)

**Timestamp:** 2026-03-22T19:34:23Z

## Task
Fixed test data accumulation and test classification issues in the performance test suite.

## Changes

### Fix 1: TestDataSeeder Data Accumulation
- Added `db.Database.EnsureDeletedAsync()` before seeding to reset the database to a clean state per test
- Removed static `FirstAccountId` property to eliminate race conditions
- Modified `SeedAsync` to return `Task<Guid>` for account ID retrieval

**Rationale:** In-memory database is shared across tests in a class; without a reset, each seed appended data on top of previous seeds, causing last tests to run against 5× larger datasets than first tests.

### Fix 2: Reclassified Two CategorizationEngine Tests
- `ApplyRulesAsync_MultipleCalls_UsesCachedRules` → moved to `CategorizationEngineTests` (no timing assertion; pure cache-correctness)
- `ApplyRulesAsync_StringRulesEvaluatedFirst_RegexRulesSkippedWhenStringMatches` → renamed and moved to correctness tests (no timing assertion; asserts behavior not performance)

**Rationale:** Tests without timing assertions don't belong in Performance category (excluded from regular CI). These test correctness, not latency.

## Commit
cf62096

## Impact
- Performance tests now measure against consistent dataset sizes across test methods
- Correctness tests are no longer hidden in the Performance category
- Test data is properly isolated per test method

## Status
✓ Complete
