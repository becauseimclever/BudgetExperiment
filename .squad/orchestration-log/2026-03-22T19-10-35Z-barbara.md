# Orchestration Log: Barbara (2026-03-22T19:10:35Z)

## Task
Audited all performance test files, categorized 45 tests as 17 genuine / 28 noise

## Status
Completed (Background)

## Deliverables
- `barbara-perf-audit.md` (inbox) — 8 decisions covering performance test strategy, CI action versions, baseline management, and test reclassification
- `barbara` history.md updated with audit findings and recommendations

## Summary
Completed full audit of performance test suite across all projects. Categorized 45 tests into:
- **17 genuine performance tests:** Real-world latency assertions with meaningful thresholds
- **28 noise tests:** Vanity enum tests (12), correctness tests mislabeled as performance (2), unit tests of helper infrastructure (21)

Key findings:
1. `PerformanceWebApplicationFactory` uses EF Core InMemory by default — must switch to Testcontainers PostgreSQL (same as API tests)
2. Baseline infrastructure exists but no baseline.json committed — regression detection has never been active
3. Stress/spike tests check error rate only, missing p99 latency thresholds
4. CI workflow actions pinned to non-existent versions (v6, v7 don't exist)
5. Scenario hardcoded dates drift from real data over time

Documented 8 actionable decisions with rationale and impact analysis.
