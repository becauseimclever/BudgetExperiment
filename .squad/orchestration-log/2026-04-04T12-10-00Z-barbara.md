# Orchestration Log: barbara

**Timestamp:** 2026-04-04T12:10:00Z
**Agent:** Barbara (Tester)
**Feature:** 127 — Enhanced Charts & Visualizations
**Slice:** 2 — ChartDataService RED-phase test suite

## Task

Write 20 xUnit RED tests for `ChartDataService`, establishing full behavioral contracts for Lucius to implement against.

## Results

### Test File

`tests/BudgetExperiment.Client.Tests/Services/ChartDataServiceTests.cs` — 20 tests, all RED (compilation passes with method stubs; behavior not yet implemented).

### Behavioral Contracts Established

#### BuildSpendingHeatmap (4 tests)
1. Always returns 7-row outer array (Mon=0 … Sun=6), even for empty input.
2. Expense amounts stored as absolute values (negative `Amount.Amount` → positive `TotalAmount`).
3. Same day + same week → one aggregated data point (`TransactionCount` accumulated).
4. Different weeks → separate data points with distinct `WeekIndex`.

#### BuildBudgetWaterfall (5 tests)
5. Spending segments ordered by absolute amount descending after Income segment.
6. Running totals cumulative from income (each category subtracts from previous `RunningTotal`).
7. Net segment always last (`IsTotal = true`); all other segments `IsTotal = false`.
8. Net can be negative when spending exceeds income.
9. `CategorySpendingDto.Amount.Amount` (MoneyDto) provides the spending decimal.

#### BuildBalanceCandlesticks (3 tests)
10. Open = first balance chronologically; Close = last per interval.
11. `IsBullish = Close >= Open` (computed property).
12. Multiple months → multiple candlesticks ordered ascending by date.

#### BuildCategoryDistributions (8 tests)
13. Tukey's hinges quartile algorithm (Q1=150, Q3=450 for [100,200,300,400,500]).
14. IQR × 1.5 outlier detection; `Maximum` is whisker max (highest non-outlier).
15. `monthsBack` reference = max transaction date in dataset (deterministic, no wall-clock dependency).
16. One `BoxPlotSummary` per `CategoryName` (exact string equality grouping).

### Build

- Tests compile; all 20 fail at runtime as expected (RED phase confirmed).

## Status

✅ Complete — 20 RED tests delivered. Handed to Lucius for GREEN phase implementation.
