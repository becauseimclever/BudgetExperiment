# Orchestration Log: Lucius — Feature 147

**Date:** 2026-04-09  
**Agent:** Lucius (Backend Dev)  
**Feature:** 147 — Recurring Projection / Realization Accuracy  
**Status:** ✅ Complete

---

## Summary

Implemented F147 recurring projection accuracy hardening. Added optional `ISet<DateOnly>? excludeDates` parameter to `IRecurringInstanceProjector.GetInstancesByDateRangeAsync` to filter already-realized dates. Created `IRecurringQueryService` / `RecurringQueryService` in Application layer to fetch realized dates and pass as exclusion set. Updated all 6 call sites to pass `excludeDates: null` for backward compatibility.

---

## Work Completed

### Phase 1: Projector Signature & Implementation

- ✅ Updated `IRecurringInstanceProjector` interface in Domain with `ISet<DateOnly>? excludeDates` parameter
- ✅ Modified `RecurringInstanceProjector` implementation to filter occurrences when exclude set provided
- ✅ Updated all 6 call sites in Application layer (explicit `null` for backward compat)
- ✅ No breaking changes to existing tests; all 1,125+ Application tests pass

### Phase 2: Query Service & Realization Integration

- ✅ Created `IRecurringQueryService` interface in `Application/Recurring`
- ✅ Implemented `RecurringQueryService`:
  - Fetches realized transactions via `ITransactionRepository.GetByDateRangeAsync`
  - Builds `HashSet<DateOnly>` of realized dates using `Transaction.Date`
  - Passes exclusion set to `IRecurringInstanceProjector.GetInstancesByDateRangeAsync`
- ✅ Registered in DependencyInjection via `AddApplication()` extension
- ✅ Constructor includes null guards for both parameters

### Phase 3: Feature Flag & Documentation

- ✅ Added feature flag `feature-recurring-projection-accuracy` seeded as `false`
- ✅ Seeded in `FeatureFlagSeeds.cs`
- ✅ Integration tests (Testcontainers) verify INV-7 end-to-end

---

## Commits

1. **aba397c** — `feat(app): F147 recurring projection excludeDates`
   - Updated `IRecurringInstanceProjector` signature
   - Updated all 6 call sites
   - Created `RecurringQueryService` + interface
   - Feature flag seeded

2. **18334aa** — `docs(squad): update F147 doc status`
   - Archive entry created
   - Feature doc marked Done

---

## Testing Results

- **Unit Tests:** 4 projector exclusion tests (RecurringInstanceProjectorExcludeDatesTests) — ✅ all pass
- **Query Service:** 5 unit tests (RecurringQueryServiceTests) — ✅ all pass
- **Integration (Testcontainers):** 3 accuracy tests proving INV-7 — ✅ all pass
- **Regression:** 1,125+ Application layer tests — ✅ all pass

---

## Notes

- Parameter order: `(IReadOnlyList<RecurringTransaction>, DateOnly, DateOnly, ISet<DateOnly>?, CancellationToken)`
- Interface location: Domain (not Application) to preserve domain purity
- Realized date uses `Transaction.Date`, not `RecurringInstanceDate`
- All call sites use explicit `excludeDates: null` for clarity
- Constructor guards added for null parameters (Barbara's test requirements)

---

## Sign-Off

✅ Implementation complete. All tests pass. Ready for integration with downstream features (F148–150).

**Verified by:** dotnet test (5,765 passed, 0 failed, 1 skipped)
