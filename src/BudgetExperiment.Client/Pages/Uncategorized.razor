@page "/uncategorized"

@inject NavigationManager Navigation

@* Legacy route redirect — bookmarks to /uncategorized now land on the unified Transactions page. *@

@code {
    /// <inheritdoc />
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/transactions?uncategorized=true", replace: true);
    }
}
